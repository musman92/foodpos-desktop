<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FoodPos.App.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel
            MaxWidth="420"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="12"
            Padding="24">

            <TextBlock Text="FoodPOS Offline" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="Sign in to manage your shop settings." Opacity="0.7" TextWrapping="Wrap" />

            <TextBox x:Name="UsernameBox" Header="Username" Text="admin" />
            <PasswordBox x:Name="PasswordBox" Header="Password" />

            <TextBlock x:Name="ErrorText" Foreground="IndianRed" TextWrapping="Wrap" Visibility="Collapsed" />

            <Button
                x:Name="LoginButton"
                Content="Sign in"
                HorizontalAlignment="Stretch"
                Style="{StaticResource AccentButtonStyle}"
                Click="LoginButton_Click" />

            <TextBlock Opacity="0.5" FontSize="12" Text="Default: admin / admin123" />
        </StackPanel>
    </Grid>
</UserControl>
