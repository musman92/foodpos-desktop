[build]
target-dir = "target"

# Keep debug builds smaller so `tauri dev` fits on low-disk machines.
[profile.dev]
debug = 1
incremental = true
split-debuginfo = "unpacked"

[profile.dev.package."*"]
opt-level = 0
debug = 1
