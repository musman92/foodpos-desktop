[package]
name = "license-gen"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "CLI to generate single-use license keys for the PoC"

[[bin]]
name = "license-gen"
path = "src/main.rs"

[dependencies]
licensing = { workspace = true }
clap = { version = "4", features = ["derive"] }
